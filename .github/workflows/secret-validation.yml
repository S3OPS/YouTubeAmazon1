name: Secret Validation

on:
    schedule:
        # Validate secrets weekly on Wednesdays at 10 AM
        - cron: '0 10 * * 3'
    workflow_dispatch:
    pull_request:
        paths:
            - '.env.example'
            - 'server.js'
            - 'setup.js'

permissions:
    contents: read
    issues: write

jobs:
    validate-secrets:
        name: Validate Secret Configuration
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v6

            - name: Setup Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: '18'

            - name: Validate .env.example structure
              run: |
                  echo "🔍 Validating .env.example structure..."

                  # Check required variables exist
                  REQUIRED_VARS=(
                    "YOUTUBE_CLIENT_ID"
                    "YOUTUBE_CLIENT_SECRET"
                    "YOUTUBE_REFRESH_TOKEN"
                    "AMAZON_AFFILIATE_TAG"
                    "PORT"
                    "NODE_ENV"
                  )

                  MISSING=()
                  for var in "${REQUIRED_VARS[@]}"; do
                    if ! grep -q "^${var}=" .env.example; then
                      MISSING+=("$var")
                    fi
                  done

                  if [ ${#MISSING[@]} -gt 0 ]; then
                    echo "❌ Missing required variables in .env.example:"
                    printf '%s\n' "${MISSING[@]}"
                    exit 1
                  else
                    echo "✅ All required variables present in .env.example"
                  fi

            - name: Check for exposed secrets in code
              run: |
                  echo "🔍 Scanning for exposed secrets..."

                  # Patterns to search for
                  PATTERNS=(
                    "YOUTUBE_CLIENT_SECRET.*=.*['\"][^'\"]{20,}"
                    "YOUTUBE_REFRESH_TOKEN.*=.*['\"][^'\"]{20,}"
                    "api[_-]?token.*=.*['\"][^'\"]{20,}"
                    "Bearer [a-zA-Z0-9_-]{20,}"
                    "client[_-]?secret.*=.*['\"][^'\"]{20,}"
                  )

                  FOUND=0
                  for pattern in "${PATTERNS[@]}"; do
                    if grep -rE "$pattern" --include="*.js" --include="*.html" --exclude-dir=node_modules . | grep -v ".env.example" | grep -v "test-"; then
                      echo "⚠️  Found potential secret exposure"
                      FOUND=1
                    fi
                  done

                  if [ $FOUND -eq 0 ]; then
                    echo "✅ No exposed secrets found in code"
                  else
                    echo "❌ Potential secrets found in code!"
                    exit 1
                  fi

            - name: Validate server.js uses environment variables
              run: |
                  echo "🔍 Validating server.js uses env vars..."

                  # Check that server.js uses process.env for YouTube API
                  if grep -q "process\.env\.YOUTUBE_CLIENT_ID\|process\.env\.YOUTUBE_CLIENT_SECRET" server.js youtube-api.js 2>/dev/null; then
                    echo "✅ Server uses YouTube credentials from env"
                  else
                    echo "⚠️  YouTube credentials not found in expected files"
                  fi

                  # Check for Amazon affiliate tag
                  if grep -q "process\.env\.AMAZON_AFFILIATE_TAG" server.js amazon-affiliate.js 2>/dev/null; then
                    echo "✅ Server uses Amazon affiliate tag from env"
                  else
                    echo "⚠️  Amazon affiliate tag not found in expected files"
                  fi

            - name: Check .gitignore includes .env
              run: |
                  echo "🔍 Checking .gitignore..."

                  if grep -q "^\.env$" .gitignore; then
                    echo "✅ .env is in .gitignore"
                  else
                    echo "❌ .env is NOT in .gitignore!"
                    exit 1
                  fi

            - name: Validate no .env file in repository
              run: |
                  echo "🔍 Checking for .env file in repository..."

                  if [ -f .env ]; then
                    echo "❌ .env file found in repository!"
                    echo "This should never be committed!"
                    exit 1
                  else
                    echo "✅ No .env file in repository"
                  fi

            - name: Check for hardcoded URLs
              run: |
                  echo "🔍 Checking for hardcoded API URLs..."

                  # Look for hardcoded YouTube/Google API URLs that should be in env
                  if grep -r "https://www.googleapis.com/youtube" --include="*.js" . | grep -v "\.env\.example" | grep -v "setup" | grep -v "README" | grep -v "\.md"; then
                    echo "⚠️  Found hardcoded YouTube API URLs"
                    echo "Review if these should use environment variables"
                    # Don't fail, just warn (some URLs are expected in API libraries)
                  else
                    echo "✅ No unexpected hardcoded API URLs found"
                  fi

            - name: Create issue on validation failure
              if: failure()
              uses: actions/github-script@v8
              with:
                  script: |
                      const issue = await github.rest.issues.create({
                        owner: context.repo.owner,
                        repo: context.repo.repo,
                        title: '🔐 Secret Validation Failed',
                        body: `## Secret Validation Failure
                        
                        The automated secret validation detected security issues.
                        
                        **Workflow Run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
                        **Time:** ${new Date().toISOString()}
                        
                        **Possible Issues:**
                        - Missing required environment variables in .env.example
                        - Exposed secrets in code files
                        - Hardcoded credentials or tokens
                        - .env file committed to repository
                        - .env not in .gitignore
                        
                        Please review the workflow logs and fix the security issues immediately.`,
                        labels: ['automated', 'security', 'secrets', 'urgent']
                      });
                      console.log('Created issue:', issue.data.number);

            - name: Summary
              if: success()
              run: |
                  echo "## ✅ Secret Validation Passed"
                  echo ""
                  echo "All secret configuration checks passed:"
                  echo "- ✅ .env.example has all required variables"
                  echo "- ✅ No secrets exposed in code"
                  echo "- ✅ server.js uses environment variables"
                  echo "- ✅ .env is in .gitignore"
                  echo "- ✅ No .env file in repository"
